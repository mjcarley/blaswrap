## Process this file with automake to produce Makefile.in

# AUTOMAKE_OPTIONS = readme-alpha

ACLOCAL_AMFLAGS = -I m4

DIST_SUBDIRS = src tests

SUBDIRS = src tests

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = blaswrap.pc
